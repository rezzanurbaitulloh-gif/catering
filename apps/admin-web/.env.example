# Salin ke .env.local (jangan commit .env.local — sudah di-gitignore root via .env.*)
NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...
# Tidak ada SERVICE_ROLE di app ini. Semua akses lewat RLS + anon key.
# Mode demo: tanpa login, dashboard tetap tampil dari tabel publik; tabel privat
# menampilkan status "login required" yang ramah.
